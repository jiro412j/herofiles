import { SimpleChanges } from '@angular/core';
export declare class HotSettingsResolver {
    private hooks;
    private options;
    mergeSettings(component: any): object;
    prepareChanges(changes: SimpleChanges): object;
}
